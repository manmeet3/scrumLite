{{extend 'layout.html'}}
<h1>Manage Team</h1>
{{=form}}
{{for row in rows:}}
<button onclick=URL('removemember',vars=dict(id=row))>Remove</button>
{{=person[row].first_name}}
{{=person[row].last_name}}
{{=person[row].email}}
<br>
{{pass}}
